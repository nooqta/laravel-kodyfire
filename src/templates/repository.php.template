<?php

namespace App\Repositories;

use Log;
use Exception;
use App\Models\\{{ name}};
use Illuminate\Support\Facades\Storage;
use Illuminate\Support\Facades\DB;

class {{ class }}
{
    public function create($data)
    {
        DB::beginTransaction();
        try {
            $model = {{classify name}}::create($data);
        } catch (\Exception $e) {
            DB::rollback();
            throw new Exception($e->getMessage());
        }
        DB::commit();
        return $model;
    }

    public function update({{classify name }} $model, $data)
    {
        DB::beginTransaction();
        try {

            $model->update($data);
        } catch (\Exception $e) {
            DB::rollback();
            throw new Exception($e->getMessage());
        }
        DB::commit();
        return $model;
    }
}
